apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-playg.fullname" . }}
  labels:
    {{- include "python-playg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-playg.selectorLabels" . | nindent 4 }}
