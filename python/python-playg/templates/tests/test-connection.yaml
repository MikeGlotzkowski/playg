apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-playg.fullname" . }}-test-connection"
  labels:
    {{- include "python-playg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-playg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
